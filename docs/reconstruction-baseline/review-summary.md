# 多源复原基线审阅摘要

## 结论

基于提交 `73fa853a4b836b78de7de640726f026c9f35ac2a` 修正后的基线已经可以由正式项目直接读取和导入：JSON 为无 BOM 标准 UTF-8，Node.js 原生 `JSON.parse()` 通过；保留 31 条 `configs.records` 证据摘要，并新增 15 类、5,708 条完整配置实体。651 条常规任务和 12 条试炼任务的开发字段类型已固定。

## 四类数据的边界

- **原始单源内容**：任务完整原值位于 `raw_source_record` / `raw_*`，配置实体完整原值位于 `raw_data`；均带来源文件、定位信息和来源提交。
- **规范化开发数据**：任务的 `targets`、`required_quantities`、`required_items`、`kill_targets` 始终为数组；配置实体使用全局唯一 `canonical_id`、原始显示名称和 `normalized_data`。
- **多源冲突**：32 条集中冲突仍与 32 个冲突主体一一对应，没有平均或覆盖冲突数值。
- **尚未核实为原作的内容**：zhsh 的单源任务和配置全部保留，但标记为 `UNVERIFIED_AS_ORIGINAL`，不视为已证明的官方原作内容。

## 来源

| 仓库 | 提交 | 状态 |
|---|---|---|
| zhsh | b841e0e7f6dfcc5ef5dccd22c42989b12847816e | 只读、零改动 |
| zhsh-game_astrbot | 807503c16e80aa9ea1c698e0fadd4e6ab5e564de | 只读、零改动 |
| dpcq | f39aa76ce4d5c95b7cf6c565d815618395eb1d39 | 只读、零改动 |
| zonghengsihai | 0b8996e8e5b7293321f9827994324d13e3c2509c | 只读、零改动 |

## 实体统计

| 实体 | 条数 | 实体 | 条数 |
|---|---:|---|---:|
| 世界分区 | 6 | 城市 | 40 |
| 地点 | 641 | 地点连接 | 627 |
| NPC 放置 | 636 | 物品 | 153 |
| 装备 | 423 | 怪物放置 | 278 |
| 掉落关系 | 2732 | 商店条目 | 63 |
| 城市商品价格区间 | 54 | 船只 | 14 |
| 鱼类 | 21 | 宠物配置段 | 8 |
| 试炼任务 | 12 | **配置实体合计** | **5708** |

旧摘要把 `insideMap.json` 误计为 475 条；实际为 `n=445`、`w=182`，共 627 条。旧摘要把 106 个价格上下界端点误称为 106 个区间；实际为 54 个城市-商品区间。这两项已与逐条实体统计统一。

## 任务与冲突

- 常规任务：651；SINGLE_SOURCE 651。类型为对话 372、打怪 142、收集 64、送物品 62、运货 11。
- 试炼任务：12；原始 `index=1000–1011`，运行时代码改写为 `2000–2011`，两者均保留。
- task15/index=274：原始目标 3 项、原始数量仅 `"5"`；开发值为 `[5, null, null]`，后两项未猜测，并附任务推进冲突和待办引用。
- 集中冲突：32；冲突主体：32；一一对应。

## 自动验证

`scripts/validate-multisource-baseline.js` 检查并通过：

- UTF-8 无 BOM；
- Node.js 原生 `JSON.parse()`；
- `canonical_id` 全局唯一；
- 所有记录来源完整；
- 任务前后关系存在且双向闭合；
- 任务规范化字段类型固定；
- 15 类实体条数与证据摘要一致；
- 32 个冲突与 32 个主体一一对应。

机器可读结果位于 `validation-summary.json`。正式游戏代码未改动，参考仓库未改动。
